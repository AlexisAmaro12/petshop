{{> navadmin}}
<div class="container p-4">
    <div class="row">
        <div class="col-md-8 mx-auto">
            <div class="card text-center">
                <div class="card-body">
                    <h3>Bienvenido {{user.rol}}</h3>
                    <h4>{{user.fullname}}</h4>
                </div>
            </div>
        </div>
    </div>
</div>